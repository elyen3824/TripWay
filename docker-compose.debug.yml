version: '2'

services:
  tripwaycsharp:
    image: tripwaycsharp:debug
    build:
      context: .
      dockerfile: Dockerfile.debug
    ports:
      - "80:80"
    environment:
      - REMOTE_DEBUGGING
